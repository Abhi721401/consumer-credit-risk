# requirements.txt — core runtime dependencies.
# Used by both Docker images (API + dashboard) and by anyone just
# serving/scoring the already-trained model. Does NOT include the
# training-only heavyweight libraries (torch, shap, matplotlib,
# imbalanced-learn) — for the full pipeline (retraining, EDA, SHAP,
# the VAE experiment), install requirements-train.txt instead.

# pandas>=2.0
# numpy>=1.24
# scipy>=1.10
# scikit-learn==1.8.0
# xgboost>=2.0
# joblib>=1.3
# streamlit==1.63.0
# plotly>=5.18
# groq>=1.0
# python-dotenv>=1.0
# fastapi>=0.110
# uvicorn[standard]>=0.29
# pydantic>=2.6
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn==1.8.0
xgboost==3.4.1
joblib>=1.3
streamlit==1.63.0
plotly>=5.18
groq>=1.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6